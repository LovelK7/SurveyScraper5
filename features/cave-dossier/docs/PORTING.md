# Porting ledger — code copied from crospeleo-automation

Rule ([CLAUDE.md](../../../CLAUDE.md)): code may be COPIED from the read-only
`../crospeleo-automation` repo and adapted freely; every copy is logged here.
Source paths are under `crospeleo-automation/src/crospeleo_automation/`.

| Date | Source | Dest (`src/cave_dossier/`) | What changed |
|---|---|---|---|
| 2026-08-16 | `services/normalization.py` | `core/normalization.py` | Verbatim (diacritic-folding `normalize_lookup_key`, `cleanup_whitespace`, `split_semicolon_values`, `parse_optional_float`). |
| 2026-08-16 | `services/sb_safe_io.py` | `sb/safe_io.py` | Near-verbatim: kept lock-file preflight, `SBWorkbookUnreachable` + three-mode Drive-offline diagnosis, daily-rotated backups, xlwings COM single-cell write (dormant until M6 — xlwings not installed). Docstring paths adjusted; `[sb-write]` extra name kept. |
| 2026-08-16 | `services/sb_loader.py` | `sb/loader.py` | Kept: `_read_sheet` (header=None read + preflight), `_detect_header_row` scoring, `_canonicalize_columns`, `_header_text`, `_resolve_sheet_name`, `_cell_as_text`, `__excel_row_number`, openpyxl data-validation warning suppression. **Stripped:** queue/round logic (`load_queue`, `RoundProgress`, `_is_target_round`, `_is_marked_as_*`), `SubmissionLedger`, dossier seeding, `Georeference`. **New:** `SBReader` API — `sheet_names()`, `load_rows()`, `describe_columns()`, `find_caves(query)` (name/SUE/plaque, diacritic-insensitive exact + name-substring fallback), `stats()`, `CaveRow` dataclass. |
| 2026-08-16 | `docs/EXCEL_WORKBOOK_SAFETY.md` | `docs/EXCEL_WORKBOOK_SAFETY.md` | Copied with a provenance header; module paths in it refer to crospeleo-automation, principles apply verbatim to `sb/safe_io.py`. |
| 2026-08-25 | `models/dossier.py` | `dossier/model.py` | **Restructured, not copied.** Kept: the two-tier warning/blocker idea, `ArchiveFile`/`FileRole`/`Georeference`, the `effective_*` resolver methods (vertical-difference→depth fallback), pydantic v2 models. **Dropped:** everything CroSpeleo-submission-specific (`UnknownRegistryReport`, `ManualChoiceField`, `NameCollisionReport`, `CroSpeleoSubmissionResult`, locality enrichment, ML name-origin). **New:** `Source` provenance set + `gathered`/`has()` (this dossier is filled in milestone by milestone, so "not looked at yet" must be distinguishable from "missing"), `SurveyResult` (2.1a handoff), `QueueFlag` (SB v3.0 Napomena), `UncheckedRule`. Field names kept identical where they exist on both sides. |
| 2026-08-25 | `services/readiness_validator.py` | `dossier/gating.py` | Kept the Protokol v6 substance: Tablica 2 mandatory-field manifest, §5.1 year-conditional GPS/foto/pločica rules with the 2015 threshold, §5 kaverna plaque exemption, per-author izjava gating (the SUE 575 lesson), negative-dimension guard, malformed-year warning. **Restructured** from four hardcoded field tuples into one `RULES` table where each rule declares its `sources` **and its `level`**; a rule whose source is not gathered becomes `unchecked` instead of a blocker. The single crospeleo gate became **two** (2026-08-26): gate 1 = the society's katastarski-broj step, gate 2 = CroSpeleo. Consequence: crospeleo's mandatory `archive_reference` (SUE number) moved to gate 2 only — in this tool it is what gate 1 *produces*. **Dropped:** plaque-registry society lookup, unknown-registry blockers, profile flags (`osz_required`, `require_statements`). |
| 2026-08-25 | `services/person_resolver.py` (`flatten_team_members`) | `core/people.py` | Separator regex kept verbatim (`[,;/&+]` + `i`/`te`/`and` as words). **New:** `/`-style placeholder tokens dropped (SB writes a bare "/" for "no author"), and initials-only fragments re-glued to the preceding name so the legacy surname-first form `Malez, M. (1960)` stays one person. |
| 2026-08-25 | `cli/dossier_report.py` | `dossier/report.py` | Same section order (identity → data → verdict last), rewritten for this dossier: sources-gathered block, SB block, "not checked yet" list. Numbers no longer print through `:g` — a 7-digit HTRS96 northing came out as `5.05e+06`. |
| 2026-08-29 | `georef/models.py` | `georef/models.py` | Near-verbatim (part 2.1c). Kept the integer-metre `coordinate_variants` (SUE 960 lesson). Dropped `existing_record` (no submission dossier to carry one). |
| 2026-08-29 | `georef/selectors.py` | `georef/selectors.py` | Verbatim (quote-aware inline-comment stripper, `SelectorRegistry`); import repointed to `core/normalization.cleanup_whitespace`. |
| 2026-08-29 | `config/selectors.yaml` | `config/selectors.yaml` | Verbatim copy of the live-DOM selector set as of 2026-08-29, comments trimmed to the load-bearing ones. |
| 2026-08-29 | `georef/client.py` | `georef/client.py` | Near-verbatim: login flow, `*_or_fallback` helpers with the 1 s primary-probe calibration, tracing, page logging. Settings now come from `core/config.py` (same field names). |
| 2026-08-29 | `georef/flows.py` | `georef/flows.py` | Verbatim minus imports: point flow, the 45 s record wait + marker-click popup retrigger, TK25 tile wait, marker-centered square crop with DPR-aware Python-side cropping. **New:** `save_png_under_limit` — 1 MB delivery budget; tries truecolor, then 256-color palette (near-free on the TK25 scan, keeps the full ~1017 px capture), downscales 15 % steps only as a last resort. Capture window is settings-sized (default 2560×1600, client.py). |
| 2026-08-29 | `georef/artifacts.py` | `georef/artifacts.py` | Kept the artifact set + `persist_georef_result`. **Changed layout:** no run machinery here, so artifacts land under gitignored `runs/georef/<padded Redni broj>/`, overwritten on re-run. |
| 2026-08-29 | `services/georef_worker.py` | `georef/worker.py` | **Restructured.** Kept the login→flow→persist orchestration shape. Dropped the dossier/readiness bookkeeping and the OSZ/stored-georef coordinate fallbacks (SB is the only source here). **New:** Redni-broj-only lookup (`find_by_serial`), delivery of `<4-digit padded Redni broj>.png` into the shared `!!Isječci karte` Drive folder, and the collated `!georef_zapisi.csv` (one row per cave, upserted by Redni broj; crospeleo keeps per-run `georef_record.txt` instead). |
| 2026-08-30 | `locality/models.py` | `geo/models.py` | Near-verbatim `NamedPlaceHit` + `AdminPlacement` (part 2.1b). **Dropped** `LocalityResult` (an OSZ-correction review payload); **new** `LocalityFinding` / `ElevationFinding` — this tool synthesizes values for a possibly empty SB row, so each field carries its own provenance (`sb` / `geo-admin` / `geo-rgi`). |
| 2026-08-30 | `locality/toponym_matcher.py` | `geo/toponym_matcher.py` | Verbatim, including the docstring lesson distinguishing ImportError (rapidfuzz absent → difflib) from score-below-cutoff (the Nina/Dino Grozić bug, object 527). |
| 2026-08-30 | `locality/rgi_client.py` | `geo/rgi_client.py` | Near-verbatim WFS client (fail-soft `[]`, Euclidean distances on native EPSG:3765). **New:** offline fallback (user decision 2026-08-30) — when the WFS is unreachable, the same radius query runs against locally provisioned `rgi_named_places.gpkg` via geopandas + sindex (query pattern from crospeleo `src/ml/rgi_lookup.py`, which was NOT otherwise ported); `requests` import made lazy. |
| 2026-08-30 | `locality/admin_lookup.py` | `geo/admin_lookup.py` | Adapted: kept lazy geopandas, the stale `.gpkg-journal` temp-copy workaround, `nearby_naselje_names`, fail-soft `source="unavailable"`. **Simplified:** one point-in-polygon over `naselja.gpkg` resolves naselje + općina + županija at once (the converted file carries `JLS_IME`/`ZU_IME`); `jls.gpkg`/`zupanije.gpkg` remain as per-level fallbacks. |
| 2026-08-30 | `locality/locality_enricher.py` | `geo/locality.py` | **Restructured** from a parsed-OSZ corrector into a stand-alone synthesizer (`LocalityFinder.locate`) under the 2.1b SB-wins precedence rule: SB values are annotated, never replaced; empty ones are filled from DGU/RGI. Ported verbatim: `_NON_LOCALITY_VRSTA_KEYWORDS` (object 525's chapel), `is_topographic_locality_feature`, the 5 km naselje screen (Veli Brgud), the 1.5 km locality append radius, the asymmetric 75/85 cutoffs. Dropped the dossier mutation + WarningCode plumbing. |
| 2026-08-30 | `scripts/fetch_rgi.py` | `geo/provision.py` (`_fetch_rgi`) | Kept the paged-GetFeature core: page size 5000 + startIndex, Session + urllib3 Retry (5×, backoff 2), 1 s politeness pause, per-page JSON persistence for `--resume`. Dropped the layer-discovery/DescribeFeatureType tooling and the hits-count preflight (the short-page condition ends the loop). |
| 2026-08-30 | `scripts/convert_dgu_shapefiles.py` | `geo/provision.py` (`_convert_shapefiles`) | Kept the NA/JLS/ZU keep-column tables and the `buffer(0)` geometry heal; input moved to `<data_dir>/temp/`, output reprojected to EPSG:3765 when needed. **New (no crospeleo counterpart):** `_convert_inspire_au` — admin boundaries from the open INSPIRE AU GML zip (the RPJ shapefiles crospeleo used have no open direct download): a streaming lxml parse, because GDAL's GML driver drops both the level (xlink:title attribute) and the name (nested `gn:text`); verified against the live 600 MB archive 2026-08-30. |

Planned ports (rest of M2+): `services/name_resolver.py` + `services/statement_checker.py`
(per-author izjava matching — `dossier/gating.py` uses a provisional substring match until
they land), `services/drive_resolver.py` (archive intake), OSZ FieldSpec tables from
`services/osz_parser.py` as output validator (M4). ~~`georef/` package (M3)~~ — ported
2026-08-29, rows above. ~~`locality/` package~~ — ported 2026-08-30, rows above.

Internal move, logged for traceability (not a crospeleo port): the OSZ fill
primitives from `osz-template/tools/make_mockup.py` became
`src/cave_dossier/osz/writer.py` on 2026-08-30 (2.1b). The workbench script
stays frozen for template QA; `osz/writer.py` is the maintained copy and adds
`embed_png` (isječak karte into the v10 frame). The elevation finder
(`geo/elevation.py`) is NEW code — neither repo fetched elevation before
(CroSpeleo derives it server-side, protocol v6 §6).
