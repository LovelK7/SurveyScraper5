"""The local dashboard (`cavedossier gui`): one page over every stage.

``catalog`` is the table of runnable actions, ``state`` reads the workspace and
the caves in work, ``jobs`` runs tools with live output, ``server`` is the
localhost HTTP surface and ``static/`` the page itself.
"""
