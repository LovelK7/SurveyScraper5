# cSurvey knowledge base (AI-oriented) — the `reference/` zone

**This is Zone 1 (architecture): how cSurvey the software works.** It is *reference* — stable knowledge
about the codebase, not project state. For the project's current state and what's being worked on, see
[`decisions/`](../decisions/roadmap-decisions.md) and [`projects/`](../projects/README.md); for the whole
feature layout and the develop→productionize loop, see [`README.md`](../README.md).

This documentation set was generated by a multi-agent deep-dive of the codebase (July 2026) and adversarially fact-checked against the source — claims carry `path:line` citations written `cSurvey/cSurveyPC/...`, resolving against the read-only cSurvey reference clone (a sibling of this repo). It exists so that any AI agent (or human) can (a) diagnose user-reported problems and point to the responsible code, and (b) build automation on top of cSurvey — the target scenario being **TopoDroid import → automated final sketch → exported map**, eventually via an MCP server.

Start with [`cSurvey/CLAUDE.md`](../../../../cSurvey/CLAUDE.md) (in the read-only cSurvey reference clone) for the one-page orientation, then drill into the docs below.

## Reading order

### Foundation (how the program works)
1. [human-workflow-and-glossary.md](human-workflow-and-glossary.md) — what a caver actually does with this program, domain vocabulary mapped to code names, the six demo tutorial stages decoded, machine-translation traps in the English manual.
2. [data-model-and-file-format.md](data-model-and-file-format.md) — the `cSurvey` object graph, the XML serialization convention, and the full annotated `_data.xml` schema (verified against three example surveys). **The single most important doc for automation.**
3. [calculation-engine.md](calculation-engine.md) — how shots become coordinates: the external therion.exe round-trip, invalidation/trigger chain, plan and extended-profile projections, splay/LRUD side measures, loop closure, geographic anchoring.
4. [drawing-engine.md](drawing-engine.md) — designs, the 7 fixed layers, item taxonomy, point geometry and the `S<guid>` centerline binding, warping (the core invariant), programmatic item creation, and the existing splay→wall generator.

### Pipelines (how data flows in and out)
5. [topodroid-import.md](topodroid/topodroid-import.md) — every TopoDroid touchpoint: supported artifacts (.csx/.th/.th2 — NOT the .zip project), the three-stage fix-up chain, symbol mapping, merge dialog options, recent bugfixes, what gets dropped.
   - [topodroid-zip-and-csx-format.md](topodroid/topodroid-zip-and-csx-format.md) — the TopoDroid (sending) side, grounded in the TopoDroid source at a pinned commit: project .zip internals (manifest, `survey.sql`, binary `.tdr` sketches), the phone SQLite schema, and an annotated line-by-line reconstruction of the csx XML TopoDroid exports. **The spec for the Stage-4 zip→csx pre-converter and for synthesizing test fixtures.**
6. [rendering-and-plot.md](rendering-and-plot.md) — the paint pipeline (screen/print/SVG share one engine), draw caches and their options-instance keying, highlights, centerline plot rendering.
7. [exports-and-printing.md](exports-and-printing.md) — therion .th/.th2/thconfig export (the path to finished PDF maps), SVG writer, raster export, printing; which exporters are headless-ready.

### Surroundings
8. [ui-map.md](ui-map.md) — frmMain2 anatomy, a ~30-row routing table from user gesture to domain call, the drawing-tool state machine, grid→recalc chain, build/dependency notes.
9. [3d-and-surface.md](3d-and-surface.md) — the Holos 3D viewer, cave_model native mesh generator, terrain/DEM/WMS, Loch interop.
10. [automation-surface.md](automation-surface.md) — CLI parameters (only a filename!), the internal scripting engine, the Friend accessibility wall, headless blockers, and the compared MCP architectures with a recommendation.

### Deep dives (the automation goal)
11. [topodroid-end-to-end-trace.md](topodroid/topodroid-end-to-end-trace.md) — execution-level trace of the TopoDroid import, UI click to final `_data.xml` state, with a minimal programmatic call sequence.
12. [auto-sketch-feasibility.md](auto-sketch-feasibility.md) — inventory of every facility that generates drawing geometry from measurement data, and concrete pipeline designs for "TopoDroid zip in → finished plan+profile sketch out" with honest quality expectations.
13. [mcp-blueprint.md](mcp-blueprint.md) — the actionable MCP server design: architecture choice, tool list, staged implementation roadmap, risks.

### Project state (lives outside this zone)

The docs above describe *the software*. To know what's actually being **done** — which pipeline is
targeted and why, what's verified vs assumed, the current critical path — read the strategy log and the
active projects, not these reference docs:

- [decisions/roadmap-decisions.md](../decisions/roadmap-decisions.md) — **start here if you're picking up
  the automation work.** Dated strategy decisions, the findings behind them, and corrections to the docs above.
- [projects/README.md](../projects/README.md) — the work board: every task brief + its implementation log + runs.

## Verification status

Every doc was written by a dedicated exploration agent with mandatory code grounding, then passed an independent adversarial fact-check that verified the 12–18 most load-bearing claims against the source and fixed errors in place. Corrections applied per doc: data-model 9, calculation 6, topodroid-import 3, drawing-engine 7, rendering 6, exports 4, ui-map 2, automation 4, 3d-surface 1, human-workflow 7, end-to-end-trace 6, auto-sketch-feasibility 3, mcp-blueprint 8. Claims the checkers could not fully establish are marked `(unverified)` or `(inferred)` inline — treat those with appropriate suspicion.

## Key facts that survived every check (safe to rely on)

- `.csz` = ZIP with `_data.xml` + binary assets; `.csx` = bare XML; all object cross-references are plain strings/GUIDs — out-of-process manipulation is viable. Stage-0 read-only tooling needs no build at all.
- Network solving is delegated to **external `therion.exe`** (`therion.path` user setting); the "Internal" calculation type is dead code (setter hard-codes Therion).
- TopoDroid interchange = TopoDroid's **exported .csx** (best; auto-fixed and sketch-converted on load via `LoadOptionsEnum.FixTopoDroid`), or therion .th/.th2. The TopoDroid .zip project archive is not readable.
- The proven headless import sequence (used by the UI itself, frmMain2.vb:11860-11865): `New cSurvey` → `Load(path, FixTopoDroid)` → `Invalidate()` → `Calculate.Calculate(True)` → `SaveTo`. Load/SaveTo/Invalidate are Public; a purely Public driver can already do csx→csz conversion because Load runs the calculation and binding internally.
- Plan cave walls can already be auto-generated from splays: `modSegmentsTools.CreatePlanBorderFromSplay` (cSurvey/cSurveyPC/modSegmentsTools.vb:424) — but it is debug-gated in the UI and **never binds its output** (must call `BindSegments()` after, or the border won't warp). The profile counterpart is an empty stub — the main missing piece for full auto-sketch.
- LRUD-based alternatives are useless on phone-surveyed data: real imports (see `buless.csz`, which is PocketTopo-derived, 267/316 splays) carry **no LRUD values at all**; nothing in the codebase derives LRUD from splays.
- The domain core is headless-capable but sits behind `Friend` accessibility and WinForms-initialized settings; the recommended seam is a net48 worker host + a ~100-line Public facade (`cAutomation`) inside cSurveyPC, fronted by a modern-.NET MCP stdio process (see mcp-blueprint.md). No `InternalsVisibleTo` exists today.
- Compile blocker for anything beyond Stage 0: DevExpress v24.2 GAC references + licenses.licx.

## Known gaps (from the completeness audit — undocumented, expand on demand)

A final audit tested the docs against realistic diagnosis/automation scenarios; all passed. These subsystems remain undocumented — none block the TopoDroid automation goal, but a user report touching them has no doc trail yet. Pointers for whoever documents them next:

- **Autosave / file history / crash recovery** (medium) — full history engine (`HistoryModeEnum` Folder/Web/Both, autosave thread, emergency save on unhandled exception): frmMain2.vb:111-131, :1094-1240, :1146-1172, :9657+; ApplicationEvents.vb:35-43; frmExceptionManager.vb.
- **Linked surveys & master/slave** (medium) — external .csz references by lowercased path (classic "linked survey vanished after moving folders"): cLinkedSurveys.vb, cMasterSurvey.vb, frmMasterSlaveManager.vb.
- **Resurvey subsystem** (medium) — parallel `.crsx`/`.crsz` document type + editor (~7k lines) for digitizing legacy paper maps: cResurvey.vb, frmResurveyMain.vb, frmImportResurvey.vb.
- **Settings-key inventory** (low) — no consolidated table of the ~750 `GetSetting` sites (HKCU\Software\Cepelabs\cSurvey) and frmSettings map.
- **Undo internals** (low) — snapshot lifecycle, whether programmatic mutations snapshot at all: cUndo.vb, cEditTools.vb:1901-1917.
- **Cave register + web services** (low) — frmCaveRegister script bindings, web history upload, cLocalSecurity TripleDES.
- **Logging** (low) — there is **no log file**: the console dock is the only sink; therion's own `%TEMP%\_therion*_log.log` is the only persistent diagnostic artifact. (`cSurveyDiagnostics` is an empty test shell, not a crash reporter.)
