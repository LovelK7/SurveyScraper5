# Exports, Printing and Preview

## Purpose

This subsystem is how a finished cave map (and its survey data) leaves cSurvey: as Therion source files (`.th` centerline + `.th2` scraps + `.thconfig`), as SVG, as raster images (JPG/PNG/TIF/BMP), on paper via GDI+ printing, as Google Earth KML, Excel `.xlsx`, VisualTopo `.TRO`, or as a Loch 3D model (`.lox`, produced by running the external `therion.exe`). It also covers the print/export preview window (`frmPreview`) and the small `cPrintController` helper project. Everything of substance is in the `modExport` module (`cSurvey/cSurveyPC/modExport.vb`, 4104 lines) plus `modSVG`/`modMetapost`; the forms are thin option-collectors around it.

## Domain concepts

- **Therion vocabulary**: a `.th` file contains nested `survey` blocks with `centerline … endcenterline` shot data; a `.th2` file contains `scrap … endscrap` blocks of 2D drawing primitives (`point`, `line`, `area`); a `.thconfig` file tells `therion.exe` what to compile (`source` lines + `export` commands). Profile view is Therion's "extended" projection. cSurvey generates all three.
- **Station qualification**: Therion station names are qualified per sub-survey as `name@survey.path`. cSurvey emits one Therion sub-survey per cave/branch and tracks in which branch each station first appeared (`TrigpointFirstSession` dictionary); stations reused across branches get `equate` lines (cSurvey/cSurveyPC/modExport.vb:3891).
- **Name dictionary**: an optional `IDictionary(Of String,String)` passed to every data exporter that renames stations on the way out. Two producers: `TherionGetSavenameDictionary` (sanitizes names, cSurvey/cSurveyPC/modExport.vb:3132) for user-facing exports, and `CreateStationDictionary` (numeric indexes, cSurvey/cSurveyPC/modExport.vb:26) for internal therion runs ("safe names", setting `therion.trigpointsafename`).
- **Scrap export vs sketch export**: `TherionExportOptionsEnum.Scrap` converts the *cSurvey drawing* (plan/profile items) into `.th2` scraps; `ExportSketch` exports imported *raster sketches* (`cDesignSketch`, e.g. from TopoDroid) as `.th2` scraps with a `-sketch image.jpg` background (cSurvey/cSurveyPC/modExport.vb:3919).
- **Export profile / preview profile**: named, per-design bundles of rendering options. `cExportProfile` wraps a `cOptionsExport` (image size, unit, DPI, margins, scale, file format, SVG options) and is persisted inside the `.csx` XML under `exportprofiles` (cSurvey/cSurveyPC/cExportProfile.vb:12). The two system profiles are the survey options named `_export.plan` / `_export.profile` (cSurvey/cSurveyPC/cExportProfile.vb:30). The print side uses the analogous `_preview.plan` / `_preview.profile` options.
- **therion.ini / THERION env var**: cSurvey drives `therion.exe` with the environment variable `THERION` pointing at a folder containing `therion.ini` (cSurvey/cSurveyPC/modMain.vb:379); the ini is regenerated by cSurvey from user settings, it is not hand-edited (see below).

## Architecture

Two largely independent halves:

**1. Data/file exporters (headless-capable).** `modExport` is a VB module of pure `Sub Export(Survey, Filename, Options)` functions that read the loaded `cSurvey` object (segments, sessions, trigpoints, calculated coordinates, designs) and write text/XML/XLSX files. No form state is consumed; the WinForms dialogs (`frmExportTherion`, `frmExportExcel`, `frmExportGoogleKML`) only load/save checkbox defaults into application settings and are read once by the caller in `frmMain2.pSurveyExport` (cSurvey/cSurveyPC/frmMain2.vb:5662), which builds an `…OptionsEnum` flags value and calls the module function. (`frmExportVisualTopo` and `frmExportHolos` exist as files but are never instantiated — dead code; VisualTopo export runs with `VTopoExportOptionsEnum.Default` and no dialog, cSurvey/cSurveyPC/frmMain2.vb:5678-5679.)

**2. Rendering exporters (image/SVG/print) hosted by `frmPreview`.** The same design-paint engine used on screen (`cDesign.Paint(Graphics, cOptionsCenterline, …)`, see [rendering-and-plot.md](rendering-and-plot.md)) is pointed at three different `Graphics` targets: an offscreen `Bitmap` for image export (cSurvey/cSurveyPC/frmPreview.vb:976), the printer `Graphics` inside a `PrintDocument.PrintPage` handler (cSurvey/cSurveyPC/frmPreview.vb:1095), or — for SVG — bypassed entirely in favour of `cDesign.ToSvg(...)` which walks the same item tree and emits XML (cSurvey/cSurveyPC/cDesignPlan.vb:444).

**Data flow for a Therion export** (the flagship path): `frmMain2.pSurveyExport` → `modExport.TherionThExportTo(Survey, file.th, dictionary, options)` (cSurvey/cSurveyPC/modExport.vb:3791) writes the `.th` master file; while writing, per option flags it also spawns sibling files: `*.th2` scrap files per cave/branch/projection (`pTherionExportToScraps`, cSurvey/cSurveyPC/modExport.vb:2393), sketch `.th2` + `.jpg` pairs (cSurvey/cSurveyPC/modExport.vb:3923), a surface-elevation file (cSurvey/cSurveyPC/modExport.vb:3819) and a `*_thconfig.thconfig` layout file with metapost clipart definitions (`pTherionExportMapConfig`, cSurvey/cSurveyPC/modExport.vb:2863); every generated `.th2`/surface file gets an `input "file"` line in the master `.th`, and all paths are returned in `cTherionCalculateResult.Files` (cSurvey/cSurveyPC/modExport.vb:3768). Optionally `TherionCreateConfig` (cSurvey/cSurveyPC/modExport.vb:799) writes a `.thconfig` whose `export map -proj plan/extended -output *.pdf` commands make `therion.exe` produce the final PDF — **cSurvey itself has no PDF writer**.

The same exporter is reused *internally* in three places that then run `therion.exe` (`modMain.ExecuteTherion`, cSurvey/cSurveyPC/modMain.vb:363): the calculation engine (loop closure: exports temp `.th`, config with `export model -fmt compass` + two `.xvi` maps, parses the log for declination/loop errors — cSurvey/cSurveyPC/Calculate/cCalculate.vb:1663-1688), the Loch 3D viewer/`.lox` export (cSurvey/cSurveyPC/frmMain2.vb:9745), and `pSurveyExportToTherion(Commands)` (cSurvey/cSurveyPC/frmMain2.vb:9674) — the "TherionPad" ribbon buttons that compile the survey to a **finished** map/model in one click: temp `.th` with the `Scrap` flag (drawings included, frmMain2.vb:9713), `TherionCreateConfig` with the given export command (:9715), `ExecuteTherion` (:9718). Commands used: `export map -proj plan -fmt pdf|svg|dxf` (btnTherionPadExportPlan, frmMain2.vb:9953-9971), `export map -proj extended …` (btnTherionPadExportProfile, :9977-9995), `export model -fmt loch|compass|survex|dxf|esri|vrlm|3dmf|kml` (btnTherionPadExport3D, :9919-9947).

### Every export target and its entry point

| Target | Output | Code entry point | UI dialog | Headless? |
|---|---|---|---|---|
| Therion data (+drawings) | `.th`, `*.th2`, `*_thconfig.thconfig`, sketch `.jpg` | `modExport.TherionThExportTo` (cSurvey/cSurveyPC/modExport.vb:3791); legacy variant `TherionThExportTo_Version1` (:3140) | `frmExportTherion` (3 checkboxes) via frmMain2.vb:5681 | **Yes** — pure function over `cSurvey`; survey must already be calculated |
| Therion config only | `.thconfig` | `modExport.TherionCreateConfig` (cSurvey/cSurveyPC/modExport.vb:799) | — | **Yes** |
| VisualTopo | `.TRO` | `modExport.VTopoTroExportTo` (cSurvey/cSurveyPC/modExport.vb:835) | none (filter index 1 in save dialog, frmMain2.vb:5679) | **Yes** |
| Excel | `.xlsx` (EPPlus/OfficeOpenXml) | `modExport.ExcelExportTo` (cSurvey/cSurveyPC/modExport.vb:1829) | `frmExportExcel` | **Yes** |
| Google Earth | `.kml` (track, waypoints, cave borders) | `modExport.GoogleKmlExportTo2` (cSurvey/cSurveyPC/modExport.vb:544) | `frmExportGoogleKML` | **Mostly** — pops a `MsgBox` if GPS is not enabled/georeferenced (cSurvey/cSurveyPC/modExport.vb:548) |
| KML image overlay | `doc.kml` next to an exported image | `modExport.GoogleKmlOverlayExportTo` (cSurvey/cSurveyPC/modExport.vb:176) | driven from frmPreview.vb:1562 | Yes (function itself); georef math lives in the form |
| Raster image | JPG/TIF/PNG/BMP | `frmPreview.pDrawExportPreview` (render, cSurvey/cSurveyPC/frmPreview.vb:931) + `pExport` (save, :1490) | `frmPreview` in `Export` mode | **Form-entangled**, but the underlying `Survey.Plan/Profile.Paint(Graphics, cOptionsExport, …)` call (frmPreview.vb:1059) is headless-usable — build a Bitmap+Matrix yourself |
| SVG | `.svg` | `cDesignPlan.ToSvg` / `cDesignProfile.ToSvg` (cSurvey/cSurveyPC/cDesignPlan.vb:444, cSurvey/cSurveyPC/cDesignProfile.vb:446), helpers in `modSVG` | `frmPreview` (`pExport` .svg branch, frmPreview.vb:1570) + `frmPreviewSVGOptions` | **Yes at the `ToSvg` level** — the form only computes page rect/viewBox/zoom (frmPreview.vb:1579-1630) |
| Paper / PDF-via-driver | printer spool | `frmPreview.pPrint` (cSurvey/cSurveyPC/frmPreview.vb:1238) + `oDoc_PrintPage` (:1095) | `frmPreview` in `Preview` mode | **No** — WinForms `PrintDocument`/`PrintDialog`; PDF only through a PDF printer driver or through Therion |
| Loch 3D model | `.lox` (opens `loch.exe` or saves) | `frmMain2.pSurveyLoch` (cSurvey/cSurveyPC/frmMain2.vb:9745) → `TherionThExportTo` + `TherionCreateConfig("export model -fmt loch …")` (:9850) + `ExecuteTherion` (:9854) | `frmLochDialog` | Pipeline pieces headless; the wrapper is a form method with dialogs |
| Finished map/model compiled by Therion ("TherionPad") | map `.pdf`/`.svg`/`.dxf`; model `.lox`/`.plt` (compass)/`.3d` (survex)/`.dxf`/`.shp` (esri)/`.vrlm`/`.3dmf`/`.kml` | `frmMain2.pSurveyExportToTherion(Commands)` (cSurvey/cSurveyPC/frmMain2.vb:9674): temp `.th` incl. `Scrap` drawings → thconfig → `ExecuteTherion` | ribbon `btnTherionPadExportPlan`/`…Profile`/`…3D` (cSurvey/cSurveyPC/frmMain2.vb:9953/:9977/:9919) | Pipeline pieces headless (needs `therion.path` setting + therion.exe installed); wrapper is a form method with a save dialog |
| 3D mesh/image from viewport | STL/DAE/X3D/OBJ/PNG/JPG | `cHolosViewer.Export` (cSurvey/cSurveyPC/Specialized/cHolosViewer.xaml.vb:1175) via HelixToolkit `Viewport3DHelper` | plain `SaveFileDialog` inside `Export` (cHolosViewer.xaml.vb:1179); `frmExportHolos` (registry-backed options) is never shown — dead code | **No** — needs the live WPF 3D viewport |
| Grid/CSV utility exports | `.csv`/`.xlsx` of any grid | `GridExportToCSV`/`GridExportToExcel`/`GridExportTo` (cSurvey/cSurveyPC/modExport.vb:1273/1426/1560) | called from data views | No — take a `DataGridView`/DevExpress grid as input |
| Garmin track | `.trk` | commented out (cSurvey/cSurveyPC/modExport.vb:4000) | — | dead code |

### The Therion export in depth

**Encoding / UTF-8 handling.** All therion-family files are written with `TextFileEncoder = New UTF8Encoding(False)` — UTF-8 *without BOM* (cSurvey/cSurveyPC/modExport.vb:3371) — and start with an `encoding utf-8` directive generated from the encoder's WebName (cSurvey/cSurveyPC/modExport.vb:3373). Identifiers (survey names, scrap names, file names) pass through `FormatTextFor(text, BaseWithoutSpacesAndSlash)` which first calls `.Unidecode()` (Unidecode.NET) then `UnAccent`/`RemoveDiacritics` and strips punctuation (cSurvey/cSurveyPC/modExport.vb:3074-3118). The `.Unidecode()` transliteration was added by commit `0b90671` to fix crashes/invalid files when the survey name contains non-Latin UTF-8 characters; commit `0b90671` also made splay-station filtering in scraps consistent. Free text (titles, labels) instead uses `FormatTextForEnum.TherionText`: kept as UTF-8, CRLF→`<br>`, quotes doubled, wrapped in `"…"` (cSurvey/cSurveyPC/modExport.vb:3119).

**Survey data mapping** (`pTherionThExportToCaveBranch`, cSurvey/cSurveyPC/modExport.vb:3404). One nested `survey <branch>` block per cave/branch (recursion at :3688); inside it one `centerline` per session containing: `date` (:3419), `vthreshold` (:3423), declination logic depending on GPS/nord-correction mode (:3425-3465), `units length/compass/clino` + `data normal from to compass clino length left right up down` (or `data diving`/`data cartesian`, :3467-3497), `calibrate` lines (:3474), `grade` (:3499). Every shot row emits a `flags` line (:3622-3628; the emit-only-on-change check is commented out): `splay`, `duplicate`, `surface`; generic *exclude* is smuggled out as `surface` because Therion has no generic exclusion (:3626). It is followed by the measured values with LRUD (:3645). With `SegmentForceDirection`, every non-splay shot also emits `extend left/right/vertical from to` (:3653-3676). After all branches, a global `centerline` emits `extend start origin@…` (:3844), `mark … fixed/painted/temporary` (:3866), `extend ignore` for user-broken connections (:3873), `fix` with lat/long when `GPS.SendToTherion` (:3880), entrance `station … entrance` lines (:3888) and cross-branch `equate`s (:3894).

**Splays without names (commit `7a7fe49`).** Flag `TherionExportOptionsEnum.SegmentSplayWithoutName = &H8000` (cSurvey/cSurveyPC/modExport.vb:669). When set, a splay shot's *to* station is replaced by `"."` (anonymous splay) at cSurvey/cSurveyPC/modExport.vb:3618-3621 (and :3537-3541 on the oversampled path), and splay-named stations are excluded from scrap `point … station` lists (cSurvey/cSurveyPC/modExport.vb:2448). The UI checkbox is `chkExportSplayWithoutNames` in `frmExportTherion` (persisted as setting `data.export.therion.splaywithoutname`, cSurvey/cSurveyPC/frmExportTherion.vb:6) and mapped to the flag at cSurvey/cSurveyPC/frmMain2.vb:5688. Independently, `CalculateSplay` controls whether the `splay` flag is emitted at all (:3622).

**Drawing → `.th2` mapping** (`pTherionExportToScraps`, cSurvey/cSurveyPC/modExport.vb:2393). One scrap file per cave × branch × projection, named `<file>_<plan|extended>_<cave>[_<branch>].th2` (:2437). Coordinates: drawing points are Y-negated and scaled ×4 (`pTherionGetPoint`, :2260, `sScale = 4` at :2444). Content:
- station reference points `point x y station -name N@survey` for every branch station (:2457);
- `cItemFreeHandLine/Area/InvertedFreeHandArea` → `line <type>` blocks; splines are first converted to beziers (`modPaint.ToBezier`, :2475) and written as bezier control triplets (:2490-2505); line type comes from the item's pen via `GetTherionLineType` (:2324): CavePen→`wall`, GenericPen/Gradient→`contour`, Cliff→`floor-step`, Overhang→`overhang`, pen None→`border:invisible`, rock/concretion category→`rock-border`/`rock-edge`; `-reverse on` for "down" pens (:2293); inverted areas get `-outline out/in` (:2361);
- closed `FreeHandArea` items additionally emit `area <type> … endarea` referencing the line ids (:2526-2534), area type from the brush via `GetTherionAreaType` (:2274): water/sand/debris/blocks/flowstone/pebbles/else bedrock;
- `Clipart` and unknown `Sign` items → `point … u:<name>` user symbols with `-attr scale/rotate` (:2535-2563); the corresponding Metapost `def` code is generated into the layout by `modMetapost.ClipartToMetapost` (cSurvey/cSurveyPC/modMetapost.vb:102) inside `pTherionExportMapConfig` (:2876-2884); known signs map to native Therion point types via the big `GetTherionSign` switch (:2635);
- `Text` items → `point … label -text "…"` with align/scale options (:2573-2581);
- the file carries xtherion editor hints `##XTHERION## xth_me_area_adjust …` and a `scrap <name> -projection plan|extended` header (:2619-2623).

`pTherionExportMapConfig` (:2863) also writes `<file>_thconfig.thconfig`: `source`, a `layout defaultlayout` containing all metapost cliparts, `scale 1 250`, legend/statistics switches, and two `export map … .pdf` commands (:2897).

**therion.ini.** `cTherionINISettings` (cSurvey/cSurveyPC/cUIHelpers.vb:4232) is a key/value list edited in `frmSettings`, serialized to the app setting `therion.inisettings`, and materialized as `<userAppPath>/therion.ini` by `Invalidate()` (cSurvey/cSurveyPC/cUIHelpers.vb:4253); if the user didn't set `loop-closure`, it appends `loop-closure therion` (:4270). When running `therion.exe`, `ExecuteTherion` sets env var `THERION` to that folder (or to setting `therion.inipath` if set) so therion picks the ini up (cSurvey/cSurveyPC/modMain.vb:379-383; call sites cSurvey/cSurveyPC/frmMain2.vb:9717, cSurvey/cSurveyPC/Calculate/cCalculate.vb:1687). The `cSurvey/cSurveyPC/therion.ini` file in the repo is an empty placeholder shipped with the app. So therion.ini configures *therion's* behaviour (notably loop-closure method) for the runs cSurvey launches — it does not configure the export text itself.

### Print / preview pipeline

`frmPreview` (cSurvey/cSurveyPC/frmPreview.vb:18) has three modes (`PreviewModeEnum`, :46): `Preview` (printing, opened from frmMain2.vb:5776), `Export` (image/SVG, frmMain2.vb:5751), `Viewer` (read-only map). Left nav lists the plan/profile *profiles*; the side panel edits the current `cOptionsPreview`/`cOptionsExport` and every checkbox triggers a re-render.

Printing is a plain WinForms `PrintDocument` (`oDoc`, created at frmPreview.vb:292). **`cPrintController`'s role**: the C# project contains only (a) `PrintControllerNoStatusDialog`, a `PrintController` wrapper that suppresses the modal ".NET printing page N" status dialog (cPrintController/cPrintController.cs:22), installed on `oDoc` at frmPreview.vb:294, and (b) a re-implementation of `PrintPreviewControl` (cPrintController/cPrintController.cs:204) used as the on-screen preview surface (`pPreview`, cSurvey/cSurveyPC/frmPreview.Designer.vb:116) — it renders preview pages by printing into a `PreviewPrintController` (cPrintController.cs:399). It is UI plumbing, not a rendering engine.

Page layout: the printer combo is filled from `PrinterSettings.InstalledPrinters` (frmPreview.vb:336) and the page-format combo from the selected printer's `PaperSizes` (frmPreview.vb:346). For *image* export the page size presets instead come from `papersizes.xml` via the `frmParametersPageSize` flyout, which just writes `ImageWidth/ImageHeight/ImageUnit` into the `cOptionsExport` (cSurvey/cSurveyPC/frmParametersPageSize.vb:26-59); margins are edited live by `frmPreviewMargins` mutating the shared `cMargins` (cSurvey/cSurveyPC/frmPreviewMargins.vb:38).

**There is no multi-page atlas.** `oDoc_PrintPage` never sets `e.HasMorePages` (no occurrence in the codebase), so a print job is always a single sheet: the design is either fit-to-page or drawn at a fixed 1:N scale (`modPaint.GetZoomFactor` converts scale to printer units, frmPreview.vb:1141-1171), centered via a `Matrix` (frmPreview.vb:1178), then `Survey.Plan/Profile.Paint` plus the Compass/Scale/InfoBox gadgets are drawn (frmPreview.vb:1184-1212). **PDF export is not direct**: users print to a PDF printer driver (the code even forces the legacy Win11 print dialog via registry, frmPreview.vb:1223) or let Therion produce PDFs from the exported `.th/.th2/.thconfig`.

### SVG export specifics

Output document (built by `modSVG.CreateSVG`, cSurvey/cSurveyPC/modSVG.vb:239): `<svg width="Wunit" height="Hunit" viewBox="x y w h">` where the viewBox is the page rectangle converted to **meters** (1 SVG user unit = 1 m of cave; conversion `ConvertRectangleToMeters`, cSurvey/cSurveyPC/modSVG.vb:172, assumes 96 dpi for px) and back-translated so cave coordinates land on the page (frmPreview.vb:1626-1630). Namespaces: svg, xlink, `csurvey` (`http://www.csurvey.it`), plus sodipodi/inkscape when `AddInkscapeReference` (modSVG.vb:251-259). A `<defs>` element is always first (modSVG.vb:262).

Structure: if scale bar/compass/info-box are enabled, the drawing goes into a nested `<svg id="plan">` with its own viewBox and the gadgets into a sibling `<svg id="legacyobjects">` (cSurvey/cSurveyPC/cDesignPlan.vb:453-491); otherwise a single Inkscape layer `<g id="plan" inkscape:groupmode="layer">` (modSVG.vb:284). Inside, `ToSvgItem` creates a layer `g id="plandesign"` and appends one `<path>` (or `<text>`/`<image>`) per drawn item; the centerline plot is appended when `DrawPlot` is on (cSurvey/cSurveyPC/cDesignPlan.vb:475).

Option flags (`cSVGWriter.SVGOptionsEnum`, cSurvey/cSurveyPC/modSVG.vb:14): `UseStyles` dedupes brush/pen combinations into CSS classes emitted in a `<style>` block (`AppendStyles`, modSVG.vb:74; `GetItemStyle`, modSVG.vb:555); `ReuseClipart` puts clipart geometry in `<defs>` and references it with `<use>` (modSVG.vb:433, 62); `AddSourceReference` stamps each element with `csurvey:type/category/cave/branch` attributes so the SVG can be round-tripped back (modSVG.vb:227); `TextAsPath`, `Images`, `Clipping`, `ClipartBrushes` control fidelity. The flags are assembled in `frmPreview.pExport` from application settings `svg.*` (frmPreview.vb:1637-1660); per-profile `UseStyles`/`ReuseClipart` also live in `cSVGOptions` persisted in the file (cSurvey/cSurveyPC/cOptions.cMargins.vb:8). `frmPreviewSVGOptions` itself is a nearly empty flyout stub holding a `cSVGOptions` (cSurvey/cSurveyPC/frmPreviewSVGOptions.vb:4); the designer-bound controls edit it (inferred — no logic in the .vb). Output is written UTF-8, non-indented, via `XmlWriter` (frmPreview.vb:1670-1675).

## Key classes & files

| File | Class / member | Responsibility |
|---|---|---|
| cSurvey/cSurveyPC/modExport.vb | `modExport` (module) | All data-format exporters: Therion (`TherionThExportTo` :3791, `TherionCreateConfig` :799, scrap writers :2393/2907, `pTherionThExportToCaveBranch` :3404), VisualTopo (:835), Excel (:1829), KML (:544/:176), name sanitizing `FormatTextFor` :3074, option enums :651 |
| cSurvey/cSurveyPC/modExport.vb | `cTherionCalculateResult` (:3768) | Return value of therion exports: per-station diving depths + list of all files written |
| cSurvey/cSurveyPC/modSVG.vb | `modSVG` + `cSVGWriter` (:11) | SVG DOM builder: root/viewBox (:239), layers (:284), style cache (:74), clipart cache/`<use>` (:433), csurvey source refs (:227) |
| cSurvey/cSurveyPC/modMetapost.vb | `modMetapost` | Converts a `cClipart` GraphicsPath into Therion Metapost symbol code (`ClipartToMetapost` :102) for `.thconfig` layouts |
| cSurvey/cSurveyPC/cDesignPlan.vb / cDesignProfile.vb | `cDesignPlan.ToSvg` (:444) / `cDesignProfile.ToSvg` (:446) | Design-tree → complete SVG document (headless-friendly entry) |
| cSurvey/cSurveyPC/cTherion.vb | `cTherion.Import` (:828) | **Import** side of therion `.th` files (despite the name it contains no export code) |
| cSurvey/cSurveyPC/frmExportTherion.vb | `frmExportTherion` | 3-checkbox dialog persisting `data.export.therion.*` settings (design, thconfig, splay-without-name) |
| cSurvey/cSurveyPC/frmMain2.vb | `pSurveyExport` (:5662), `pSurveyLoch` (:9745) | Dispatch: file-type choice → options flags → modExport call; Loch = th export + therion.exe run |
| cSurvey/cSurveyPC/frmPreview.vb | `frmPreview` | Print/Export/Viewer window: `pDrawExportPreview` :931 (bitmap render), `oDoc_PrintPage` :1095 (printer render), `pPrint` :1238, `pExport` :1490 (save image / build SVG) |
| cPrintController/cPrintController.cs | `PrintControllerNoStatusDialog` (:22), `PrintPreviewControl` (:204) | Suppress print status dialog; scrollable preview control fed by `PreviewPrintController` |
| cSurvey/cSurveyPC/cOptionsExport.vb | `cOptionsExport` (:6) | Per-profile export options (FileFormat, ImageWidth/Height/Unit, DPI, Margins, Scale, TransparentBackground, GPS, SVG), XML (de)serialization :161/:205 |
| cSurvey/cSurveyPC/cExportProfile.vb | `cExportProfiles`/`cExportProfile` | Named export profiles stored in the survey file; system profiles = `_export.plan`/`_export.profile` (:30) |
| cSurvey/cSurveyPC/cOptionsTherion.vb | `cOptionsTherion` (:6) | Survey-level therion options (only surface-DEM options wrapper) |
| cSurvey/cSurveyPC/cOptions.cMargins.vb | `cSVGOptions` (:8), `cMargins` (:60) | UseStyles/ReuseClipart persistence; margins value object with WinForms conversions |
| cSurvey/cSurveyPC/cUIHelpers.vb | `cTherionINISettings` (:4232) | Generates `<userAppPath>/therion.ini` from settings; guarantees `loop-closure therion` |
| cSurvey/cSurveyPC/modMain.vb | `ExecuteTherion` (:363), `ExecuteTherionAsync` (:298) | Run `therion.exe` hidden, `THERION` env var, stdout capture, 120 s timeout |
| cSurvey/cSurveyPC/frmParametersPageSize.vb | `frmParametersPageSize` | Page-size presets from `papersizes.xml` → `cOptionsExport.ImageWidth/Height/Unit` |
| cSurvey/cSurveyPC/Specialized/cHolosViewer.xaml.vb | `cHolosViewer.Export` (:1175) | 3D viewport → STL/DAE/X3D/OBJ/PNG/JPG via HelixToolkit |
| cSurvey/cSurveyPC/modOpeningFlags.vb | `modOpeningFlags` | Hidden Ctrl+Shift-on-open debug flags (calc mode, warping, morphing) — affects what gets rendered/exported after load, not the exporters themselves |

## Key flows

### 1. Export Therion `.th` + drawings from the UI

1. cSurvey/cSurveyPC/frmMain2.vb:5670 — user picks `.TH` in the save dialog inside `pSurveyExport(ImportExportFormatEnum.Survey)`.
2. cSurvey/cSurveyPC/frmMain2.vb:5681-5689 — `frmExportTherion` shown; flags assembled: `SegmentForceDirection`/`SegmentForcePath` from settings, `Scrap` if "export design" checked, `SegmentSplayWithoutName` if checked, always `CalculateSplay`.
3. cSurvey/cSurveyPC/modExport.vb:3132 — `TherionGetSavenameDictionary` builds the station rename map (sanitized names).
4. cSurvey/cSurveyPC/modExport.vb:3799-3803 — `TherionThExportTo` opens the file with BOM-less UTF-8, writes `encoding utf-8`, `#csurvey <version>`, and `survey <unidecoded-name> -title "…"`.
5. cSurvey/cSurveyPC/modExport.vb:3838 — per cave/branch recursion `pTherionThExportToCaveBranch` writes nested surveys/centerlines/shots (splay `To` → `.` at :3618 when flagged).
6. cSurvey/cSurveyPC/modExport.vb:3844-3917 — global centerline: `extend start`, marks, `extend ignore`, `fix`, entrances, `equate`s.
7. cSurvey/cSurveyPC/modExport.vb:3980-3988 — if `Scrap`: `pTherionExportToScraps` writes plan+profile `.th2` files (item→line/area/point mapping) and `pTherionExportMapConfig` writes the layout thconfig; each file referenced with `input "…"`; `endsurvey`.
8. cSurvey/cSurveyPC/frmMain2.vb:5692-5697 — if "create thconfig" checked, `TherionCreateConfig` writes `<name>.thconfig` with `export map -proj plan/extended -output *.pdf`.

### 2. Print a plan (single sheet)

1. cSurvey/cSurveyPC/frmMain2.vb:5776 — `frmPreview` opened in `Preview` mode; ctor creates `oDoc` (`PrintDocument`) and wraps its controller in `PrintControllerNoStatusDialog` (frmPreview.vb:292-295); `pPreview.Document = oDoc` renders preview pages through `PreviewPrintController` (cPrintController/cPrintController.cs:417).
2. cSurvey/cSurveyPC/frmPreview.vb:1238-1246 — `pPrint`: forces legacy print dialog on Win11 (:1223), shows `PrintDialog`, then `oDoc.Print()`.
3. cSurvey/cSurveyPC/frmPreview.vb:1095-1175 — `oDoc_PrintPage`: computes design bounds `GetDesignVisibleBounds`, zoom (fit-to-page ×0.9 or fixed 1:N via `modPaint.GetZoomFactor`), builds scale+translate `Matrix` on `e.Graphics`.
4. cSurvey/cSurveyPC/frmPreview.vb:1184-1212 — `Survey.Plan.Paint(...)` (or Profile), then transform reset and Compass/Scale/InfoBox gadgets `Rebind`+`Paint` in page coordinates. No `HasMorePages` → job ends after one page.

### 3. Export SVG

1. cSurvey/cSurveyPC/frmPreview.vb:1490-1509 — `pExport` shows save dialog; extension comes from `cOptionsExport.FileFormat`.
2. cSurvey/cSurveyPC/frmPreview.vb:1574-1630 — page rect from ImageWidth/Height+margins converted to meters (`modSVG.ConvertRectangleToMeters`), zoom = fit or `1/scale`, viewBox back-translated into cave coordinates.
3. cSurvey/cSurveyPC/frmPreview.vb:1636-1660 — `SVGOptionsEnum` flags assembled from `svg.*` application settings.
4. cSurvey/cSurveyPC/cDesignPlan.vb:444-495 — `ToSvg`: root `<svg>` (modSVG.vb:239), design layer via `ToSvgItem` (:438), plot layer, optional legacy-gadget sub-svg, then `AppendStyles("plan")` and `AppendCliparts`.
5. cSurvey/cSurveyPC/frmPreview.vb:1670-1675 — document saved with `XmlWriter` (UTF-8, no indent).

### 4. Loch 3D model (`.lox`) via therion.exe

1. cSurvey/cSurveyPC/frmMain2.vb:9745-9781 — `pSurveyLoch`: requires setting `therion.path`; `frmLochDialog` decides view-vs-save; flags `Loch Or ExportSurfaceElevationsData` (:9781; +`CalculateSplay Or ExportSplay` in *simple* 3D mode when splays are shown, :9788; +`UseSubData` in advanced 3D modes, :9813).
2. cSurvey/cSurveyPC/frmMain2.vb:9801 — numeric safe-name dictionary via `CreateStationDictionary`.
3. cSurvey/cSurveyPC/frmMain2.vb:9810-9850 — `TherionThExportTo` for main + linked surveys into temp `.th` files; `TherionCreateConfig(... "export model -fmt loch -output …lox")`.
4. cSurvey/cSurveyPC/frmMain2.vb:9852-9859 — `ExecuteTherion(therion.exe, therion.inipath, "config -l log")`; on success launch `loch.exe` next to therion, or move the `.lox` to the user's chosen path (:9861-9871).

## How to modify safely

- **Keep the encoding contract**: every therion-family writer must (a) use `modExport.TextFileEncoder` (BOM-less UTF-8) and (b) emit `pGetTherionTextEncorderDef` as the first line (cSurvey/cSurveyPC/modExport.vb:3371-3375). Therion rejects files whose declared encoding disagrees with the bytes; a BOM breaks the `encoding` directive parser.
- **Identifiers vs text**: anything that becomes a therion *identifier* (survey names, scrap names, station names) must go through `FormatTextFor` with a `Base*` mode or the station dictionary; anything human-readable through `TherionText`/`TherionFile` (quoted). Mixing these up reintroduces the pre-`0b90671` UTF-8 bug.
- **`TherionThExportTo` is shared infrastructure**: the calculation engine (cSurvey/cSurveyPC/Calculate/cCalculate.vb:1673), Loch export, XVI generation and the user-facing export all call it. Changing emitted syntax can silently break loop-closure calculation (the calc parses therion's log and maps errors back to segments by line number — cSurvey/cSurveyPC/Calculate/cCalculate.vb:1726). Prefer new `TherionExportOptionsEnum` flags (note `UseSubData` and `Oversample3D` currently share the value `&H1000000`, cSurvey/cSurveyPC/modExport.vb:676-677).
- **`TrigpointFirstSession` bookkeeping** must stay exactly in sync between the shot writer and everything that later qualifies station names (`equate`, scrap `point station`, sketch stations, `extend start`); a station qualified with the wrong sub-survey produces therion "station not found" errors.
- **frmPreview render parity**: `pDrawExportPreview`, `oDoc_PrintPage` and the SVG branch each duplicate the fit/scale/translate math. If you change one (e.g. margins handling), change all three, or extract it — otherwise the preview will not match the printed/exported output.
- **Options persistence**: `cOptionsExport.SaveTo/Import` writes into the `.csx` XML; new properties need both directions plus defaults in both constructors (cSurvey/cSurveyPC/cOptionsExport.vb:161, 189, 205, 237) or old files will load with zeros (see the existing `ImageWidth = 0 → 4096` fallback :212).
- Exporters assume the survey is **already calculated** (they read `Survey.Calculate.TrigPoints(...)` — e.g. cSurvey/cSurveyPC/modExport.vb:2450); when driving headlessly, run the calculation first (see [calculation-engine.md](calculation-engine.md)).

## Gotchas

- **`cTherion.vb` is import, not export.** Despite the name, the only export logic lives in `modExport.vb`. The class parses `.th` files for import (cSurvey/cSurveyPC/cTherion.vb:828).
- **No PDF writer and no atlas.** "PDF export" means either a PDF printer driver via the standard print dialog, or Therion compiling the exported `.thconfig` (`export map … .pdf`, cSurvey/cSurveyPC/modExport.vb:2897) — the TherionPad ribbon button does the latter in one click (cSurvey/cSurveyPC/frmMain2.vb:9953). Printing is strictly one page (no `HasMorePages` anywhere).
- **`frmExportTherion` mixed setting keys**: it *loads* the thconfig checkbox from `data.export.holos.createthconfig` but *saves* to `data.export.therion.createthconfig` (cSurvey/cSurveyPC/frmExportTherion.vb:5,11) — the saved value is never read back (long-standing bug; symptom: checkbox never remembers).
- **Splay flags are two independent knobs**: `SegmentSplayWithoutName` (anonymize with `.`) and `CalculateSplay` (emit the `splay` flag). frmMain2 always sets `CalculateSplay` for user exports (:5689); the internal calc export sets it too (cCalculate.vb:1663) but never `SegmentSplayWithoutName`.
- **KML/therion exports can pop message boxes** (`GoogleKmlExportTo2` at cSurvey/cSurveyPC/modExport.vb:548) — not fully silent for automation even though they live in `modExport`.
- **Scrap coordinates are ×4-scaled and Y-flipped** relative to cSurvey design coordinates (cSurvey/cSurveyPC/modExport.vb:2260, 2444); station points in scraps use 7-decimal precision, geometry 2-decimal.
- **A scrap is skipped if it would contain ≤2 points** or an empty bounds (cSurvey/cSurveyPC/modExport.vb:2608-2616) — empty branch drawings just don't produce `.th2` files.
- **Point joins are not exported**: the therion `join` generation is intentionally disabled (comment block at cSurvey/cSurveyPC/modExport.vb:2585-2606) because exported point identity may differ from the drawing.
- **`ExecuteTherion` writes an empty line to therion's stdin and can show a "still running" prompt after 120 s** (cSurvey/cSurveyPC/modMain.vb:387-395) — use `ExecuteTherionAsync` (throws `TimeoutException` instead, :298) for unattended runs.
- **`GetTherionGradientOption`/`GetTherionLSizeOption` are dead**: both `Return ""` before their `Select Case` (cSurvey/cSurveyPC/modExport.vb:2304-2322).
- **frmPreview manual-refresh mode**: with `preview.manualrefresh` (or Ctrl/Alt held while opening) the first render paints gray until Refresh is pressed (frmPreview.vb:86, 971) — exports call `btnRefresh.PerformClick()` first (frmPreview.vb:1492) but a screenshot-based automation of this form must account for it.

## Related docs

- [topodroid-import.md](topodroid/topodroid-import.md) — where TopoDroid data and sketches enter; the sketches exported by `ExportSketch` originate there.
- [calculation-engine.md](calculation-engine.md) — the therion-based calculation that reuses `TherionThExportTo` and consumes `.plt`/`.xvi` outputs.
- [rendering-and-plot.md](rendering-and-plot.md) — `cDesign.Paint`, `cOptionsCenterline` and the Compass/Scale/InfoBox gadgets reused by print/image export.
- [data-model-and-file-format.md](data-model-and-file-format.md) — `cSurvey`, segments/sessions/trigpoints and the `.csz/.csx` container the export profiles are stored in.
- [drawing-engine.md](drawing-engine.md) — the item/layer model (`cItem`, pens, brushes) whose pen/brush types drive the therion line/area mapping.
